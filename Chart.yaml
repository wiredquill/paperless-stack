apiVersion: v2
name: paperless-stack
description: Paperless-ngx document management stack with optional AI services
type: application
version: 0.1.0
appVersion: "2.28.0"
keywords:
  - paperless-ngx
  - document-management
  - ocr
  - ai
annotations:
  charts.kontinuum.io/enabled: "true"
  artifacthub.io/changes: |
    - kind: added
      description: Initial Helm chart for Kubernetes deployment
